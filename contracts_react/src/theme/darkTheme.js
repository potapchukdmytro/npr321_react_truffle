import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            light: "#f5f5f5",
            main: "#9e9e9e",
            dark: "#616161"
        },
        text: {
            primary: "#fff"
        }
    }
});

export default theme;